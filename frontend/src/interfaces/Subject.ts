export type Subject = {
  id: string;
  name: string;
  fixedWeight: number;
};

export type SubjectResponse = {
  name: string;
  fixedWeight: number;
};

export type SubjectRequest = {
  id: string;
  name: string;
  fixedWeight: number;
};
