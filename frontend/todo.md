# refatorar uso do componente de paginação

- refotar os componentes q usam o componente paginação

# refatorar o uso do componente select

- refatorar os componentes q usam o componente select para ficar agnostico à rotas

# Criar um objeto global search

- criar um objeto para facilitar o uso do parametro search do navigate

# Criar arquivos para as respostas do uruário

- arquivo rota
- arquivo de página
- arquivo de componente
